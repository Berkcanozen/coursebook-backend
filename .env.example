# Copy to .env and adjust
PORT=4000
JWT_SECRET=replace-with-a-long-random-string
JWT_EXPIRES_IN=30d
DB_FILE=./data.db
# Comma-separated origins allowed to call the API, or * for any (dev only).
# In production set this to your frontend URL, e.g. https://you.github.io
ALLOWED_ORIGIN=*
