// Edit this after deploying the API, then commit + push.
// Local development: keep the localhost line.
// Production: set it to your Render URL, e.g. "https://coursebook-api.onrender.com"
window.COURSEBOOK_API = "http://localhost:4000";
